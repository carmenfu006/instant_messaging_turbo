<%= turbo_frame_tag dom_id(message) do %>
  <%= message.created_at.to_s(:short) %>: <%= message.content %>
  <%= button_to 'Delete', room_message_path(message.room, message), method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>